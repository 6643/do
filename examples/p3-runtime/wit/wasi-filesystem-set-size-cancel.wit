package wasi:filesystem@0.3.0-rc-2025-09-16;

interface types {
  type filesize = u64;
  enum error-code {
    access,
    already,
    bad-descriptor,
    busy,
    deadlock,
    quota,
    exist,
    file-too-large,
    illegal-byte-sequence,
    in-progress,
    interrupted,
    invalid,
    io,
    is-directory,
    loop,
    too-many-links,
    message-size,
    name-too-long,
    no-device,
    no-entry,
    no-lock,
    insufficient-memory,
    insufficient-space,
    not-directory,
    not-empty,
    not-recoverable,
    unsupported,
    no-tty,
    no-such-device,
    overflow,
    not-permitted,
    pipe,
    read-only,
    invalid-seek,
    text-file-busy,
    cross-device,
  }
  resource descriptor {
    set-size: async func(size: filesize) -> result<_, error-code>;
  }
}

interface probe {
  use types.{descriptor, error-code, filesize};
  run: async func(file: own<descriptor>, size: filesize) -> result<_, error-code>;
  cancel: async func();
}

world set-size-cancel-probe {
  import types;
  export probe;
}
