package demo:marshal-record-mixed-text-byte-u32-lists-lower@1.0.0;

interface api {
  record writing {
    code: u32,
    label: string,
    bytes: list<u8>,
    values: list<u32>,
  }

  write: func(value: writing);
}

world probe {
  import api;
  export run: func() -> u32;
}
