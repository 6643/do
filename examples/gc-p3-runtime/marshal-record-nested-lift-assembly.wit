package demo:marshal-record-nested-host@1.0.0;

interface api {
  record header {
    code: u32,
    count: u64,
  }

  record reading {
    header: header,
    status: s64,
  }

  read: func() -> reading;
}

world probe {
  import api;
  export run: func() -> u32;
}
