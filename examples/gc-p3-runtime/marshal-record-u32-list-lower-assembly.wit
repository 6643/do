package demo:marshal-record-u32-list-lower@1.0.0;

interface api {
  record writing {
    code: u32,
    payload: list<u32>,
  }

  write: func(value: writing);
}

world probe {
  import api;
  export run: func() -> u32;
  export stats: func() -> u32;
}
