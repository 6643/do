package demo:marshal-record-managed-lift-multi@1.0.0;

interface api {
  record reading {
    code: u32,
    label: string,
    note: string,
  }

  read: func() -> reading;
}

world probe {
  import api;
  export run: func() -> u32;
}
