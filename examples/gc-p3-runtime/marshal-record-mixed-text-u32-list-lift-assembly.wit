package demo:marshal-record-mixed-text-u32-list-lift@1.0.0;

interface api {
  record reading {
    code: u32,
    label: string,
    payload: list<u32>,
  }

  read: func() -> reading;
}

world probe {
  import api;
  export run: func() -> u32;
  export stats: func() -> u32;
}
