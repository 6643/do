# spec questions

本目录只保留当前尚未解决的语法设计问题。

## 当前状态

- 待讨论: 无。
- 已经确认并同步到 `doc/spec.md` 的历史问题已删除，不再作为当前数据保留。

## 新问题写法

- 一题一文件。
- 文件名使用三位编号和短 slug，例如 `082-example-conflict.md`。
- 只记录当前 `doc/spec.md` 仍未解决的冲突或歧义。
- 每个文件包含: 使用场景、当前冲突、正例、反例、推荐、推荐理由、不选的理由、选项。
- 问题解决并同步到 `doc/spec.md` 后，删除对应问题文件。
