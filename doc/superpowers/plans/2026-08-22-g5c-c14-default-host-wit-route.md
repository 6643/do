# G5c C14 Default Host/WIT Route Promotion Plan

> Execute inline with fresh evidence at each checkpoint. Preserve the dirty
> worktree and do not change the migration inventory.

**Spec:** `doc/superpowers/specs/2026-08-22-g5c-c14-default-host-wit-route-design.md`

**Goal:** Admit only the verified C14 four-level scalar-record lift/lower
descriptors through ordinary synchronous `@host_func` compilation.

## Constraints

- Reuse the manifest loader, recursive boundary validator, measured plan, and
  `GcSyncHostWitRoute` bridge.
- Keep the private `--gc-wit-marshal` route and C15/C16 default routes intact.
- Preserve scalar canonical ABIs and the no-GC-reference boundary invariant.
- Do not add generic aggregate, async, resource, ownership, or new syntax.

## Task 1: RED coverage

**Files:** `src/build/run.zig`, C14 ordinary fixtures, default-route gate scripts.

- [x] Add a focused admission test for both C14 locator mappings.
- [x] Add ordinary lift/lower fixtures whose `start` functions call the host.
- [x] Add host, equivalence, and negative gates with no private selector.
- [x] Run the gates before admission and retain the expected ARC-route RED
  failure.

## Task 2: Minimal admission

**Files:** `src/build/run.zig`.

- [x] Map the exact C14 lift and lower locators to the existing descriptor
  constants.
- [x] Run the RED gates again and inspect any generic nested-shape failure.
- [x] If needed, add only the smallest recursive generic emitter support
  required by these records; do not widen admission.

## Task 3: GREEN gates and residual wiring

**Files:** C14 default gate scripts and
`src/build/test/check_gc_g5c_residual_gate.sh`.

- [x] Require both ordinary Components to parse and validate with pinned
  `wasm-tools 1.255.0` and pass the existing Wasmtime hosts.
- [x] Require ARC/GC equivalence and fail-closed negative behavior.
- [x] Invoke all three gates from the residual baseline.

## Task 4: Documentation and repository verification

**Files:** `examples/gc-p3-runtime/README.md`, `doc/roadmap_status.md`,
`doc/pending_blocked.md`, `CHANGELOG.md`, `doc/start_here.md`,
`doc/host_abi_blockers.md`.

- [x] Record the C14 default-route evidence and retain the broader pending
  boundary.
- [x] Run shell syntax checks, focused/full Zig tests, full regression,
  residual baseline, inventory, ReleaseSmall, release smoke, and
  `git diff --check`.
- [x] Confirm the inventory remains `complete_rows=15 pending_rows=15`.
